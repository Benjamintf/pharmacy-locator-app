require('dotenv').config();const express=require('express'),cors=require('cors'),helmet=require('helmet'),morgan=require('morgan');if(!process.env.DATABASE_URL)throw new Error('DATABASE_URL is required');if(!process.env.JWT_SECRET)throw new Error('JWT_SECRET is required');const app=express();const origins=(process.env.CORS_ORIGINS||'').split(',').map(x=>x.trim()).filter(Boolean);app.use(helmet());app.use(cors({origin:(o,cb)=>!o||!origins.length||origins.includes(o)?cb(null,true):cb(new Error('CORS denied'))}));app.use(express.json({limit:'1mb'}));app.use(morgan('combined'));app.get('/health',(_,r)=>r.json({status:'ok'}));app.use('/api/auth',require('./routes/auth'));app.use('/api/pharmacies',require('./routes/pharmacies'));app.use('/api/medicines',require('./routes/medicines'));app.use((_,r)=>r.status(404).json({error:'Route not found.'}));app.use((e,_q,r,_n)=>{console.error(e);r.status(500).json({error:'Internal server error.'})});app.listen(Number(process.env.PORT||3000),'0.0.0.0',()=>console.log('API ready'));